export const CAM_API_URL: string = import.meta***REMOVED***.VITE_CAM_API_URL as string;

export const CAM_ENABLED: boolean = import.meta***REMOVED***.VITE_CAM_ENABLED as boolean;

export const GATEWAY_URL: string = import.meta***REMOVED***.VITE_GATEWAY_URL as string;

export const HASURA_URL: string = import.meta***REMOVED***.VITE_HASURA_URL as string;

export const POSTGRES_DATABASE: string = import.meta***REMOVED***
  .VITE_POSTGRES_DATABASE as string;

export const POSTGRES_HOST: string = import.meta***REMOVED***
  .VITE_POSTGRES_HOST as string;

export const POSTGRES_PASSWORD: string = import.meta***REMOVED***
  .VITE_POSTGRES_PASSWORD as string;

export const POSTGRES_PORT: number = import.meta***REMOVED***
  .VITE_POSTGRES_PORT as unknown as number;

export const POSTGRES_USER: string = import.meta***REMOVED***
  .VITE_POSTGRES_USER as string;
